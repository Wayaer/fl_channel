name: example
description: Demonstrates how to use the fl_channel plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_channel:
    path: ../
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
